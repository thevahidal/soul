meta {
  name: list-rows-with-ordering
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/tables/:tableName/rows/?_ordering=-CustomerId.LastName&_extend=CustomerId
  body: none
  auth: inherit
}

params:query {
  _ordering: -CustomerId.LastName
  _extend: CustomerId
}

params:path {
  tableName: Invoice
}

settings {
  encodeUrl: true
  timeout: 0
}
